{
  "author": {
    "email": "ngehringer@gmail.com",
    "name": "Nate Gehringer"
  },
  "description": "Core: A web browser–hosted runtime library",
  "homepage": "https://backwater.systems",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "name": "@backwater-systems/core",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/ngehringer/core.git"
  },
  "type": "module",
  "version": "1.0.0",
  "devDependencies": {
    "cpy-cli": "^3.1.1",
    "del-cli": "^3.0.1",
    "eslint": "^7.12.1"
  },
  "scripts": {
    "build": "npm run clean:build && npm run build:javascript",
    "build:javascript": "cpy '**/*.js' '../dist/' --cwd './src' --parents",
    "clean": "npm run clean:build",
    "clean:build": "del-cli dist/",
    "lint": "eslint src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
