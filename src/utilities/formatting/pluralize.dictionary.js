/**
 * A dictionary of words with irregular plural forms for `core.utilities.formatting.pluralize`
 */
const pluralizeDictionary = Object.freeze({
  has: 'have',
  is: 'are'
});


export default pluralizeDictionary;